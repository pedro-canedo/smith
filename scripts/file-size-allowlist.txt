# Files allowed past the limit in scripts/check-file-size.sh, each with the
# reason it earns the exception. This list is a backlog, not a record: the
# check fails on an entry that has dropped under the limit, so removing a name
# from here is part of the commit that shrinks the file.
#
# Every entry below is the same debt — a module that still holds more than one
# concern. Splitting them is planned work, not a discovery.

# The production module came apart into `agent/` — eleven submodules, none
# over 580 lines. What is left here is the test file, still one block covering
# all eleven. It splits along the same seams, one section per commit, and the
# explicit import header added at its top is what each of those commits chips
# away at.
crates/smith-core/src/agent/tests.rs

# `on_key` (435 lines) and `on_agent_event` (25 arms) in one impl, plus the
# slash commands split across two non-contiguous blocks.
crates/smith-tui/src/app.rs
crates/smith-tui/src/app/tests.rs

# The composition layer: layout arithmetic, tool cards, sidebar, modals.
crates/smith-tui/src/ui.rs
# Follows its module. Splitting the tests before the production file would
# leave two half-arbitrary boundaries instead of one; they come apart together
# when `ui/` gets its submodules.
crates/smith-tui/src/ui/tests.rs

# One lexer with ~50 small scanner functions. Cohesive as it stands — this is
# the entry least likely to be paid off, and splitting it into six files would
# make the state machine harder to follow, not easier.
crates/smith-tui/src/highlight.rs

# Follows its module: the read-before-overwrite gate, the path jail and four
# tools are tested together because they share the `ReadSet`.
crates/smith-tools/src/fs_tools/tests.rs
