//! Visual primitives of the Ember design system — pure functions from
//! `(theme, state)` to ratatui lines, each with its own render tests.

pub mod chips;
pub mod diff;
pub mod input;
pub mod panel;
pub mod wrap;
